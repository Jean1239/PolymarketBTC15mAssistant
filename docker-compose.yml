services:
  bot-15m:
    build: .
    container_name: polymarket-bot-15m
    restart: unless-stopped
    env_file: .env
    command: node --max-old-space-size=384 src/index.js
    mem_limit: 512m
    volumes:
      - ./logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  bot-5m:
    build: .
    container_name: polymarket-bot-5m
    restart: unless-stopped
    env_file: .env
    command: node --max-old-space-size=384 src/index5m.js
    mem_limit: 512m
    volumes:
      - ./logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
