FROM node:22-alpine AS builder
WORKDIR /build
COPY dashboard/package*.json ./
RUN npm ci
COPY dashboard/ ./
RUN npm run build

FROM node:22-alpine
WORKDIR /app

COPY src/logServer.js ./src/
COPY --from=builder /build/dist ./dashboard/dist

RUN mkdir -p logs

CMD ["node", "src/logServer.js"]
